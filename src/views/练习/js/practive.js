/* 通用 省事 */
function $(id) {
	return document.getElementById(id);
}
/* 选择难度 */

function choice(){
	var choice = $("choice");
	var simple = $("simple");
	var easy = $("easy");
	var difficult = $("difficult");
	var flag;

}
